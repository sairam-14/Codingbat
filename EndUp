public String endUp(String str) {
  
  if(str.length()>=3){
  String str1 =str.substring(str.length()-3,str.length());
  
  str1=str.substring(0,str.length()-3)+str1.toUpperCase();
  return str1;
}else{
  String str2=str.toUpperCase();
  return str2;
}

}
